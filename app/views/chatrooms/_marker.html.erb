<span data-map-target="marker"
    data-chatroom-id="<%= chatroom.id %>"
    data-latitude="<%= chatroom.latitude %>"
    data-longitude="<%= chatroom.longitude %>"
    data-title="<%= chatroom.title %>"
    hidden>
</span>